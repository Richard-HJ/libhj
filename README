# To make the library libhj in your home directory:

#autoreconf -i Copy missing auxiliary files -f consider all files obsolete
autoreconf -if
./configure --prefix=$HOME
make clean
make install

# $HOME/include and $HOME/lib are created.

# To make and install libhj for all users in the system directories:

#autoreconf -i Copy missing auxiliary files -f consider all files obsolete
#autoreconf -if
#./configure 
#make clean
#make install

# Please see the file INSTALL for more details.
